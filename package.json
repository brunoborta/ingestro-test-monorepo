{
  "name": "ingestro-test-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Import & Review SDK - Case Study Implementation",
  "scripts": {
    "build": "pnpm --filter \"@ingestro/*\" run build",
    "dev": "pnpm --filter demo dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.9",
    "@types/node": "^20.17.10",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "packageManager": "pnpm@9.15.2",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
